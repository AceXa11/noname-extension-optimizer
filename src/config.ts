import { menu_center } from "./menu";
import { glass_ui, increase_contrast_ui, dialog_ui } from "./ui";
import { lib, game, _status } from "noname";

export const config = {
    "menu_center": menu_center,
    "glass_ui": glass_ui,
    "increase_contrast_ui": increase_contrast_ui,
    "dialog_ui": dialog_ui
};